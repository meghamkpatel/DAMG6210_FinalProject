

----Indexes-------------------------------------

CREATE INDEX idx_genre_name ON genre (genreName);

Create index idx_movie_id ON watch_history (movieID);

CREATE INDEX idx_movie_Title on Movie (movieTitle);

