

----Indexes
CREATE INDEX idx_genre_name ON genre (genreName);

Create index inx_movie_id ON watch_history (movieID);

CREATE INDEX idx_movie_id on genre (movie_id);

CREATE INDEX inx_movie_Title on Movie (movieTitle);

